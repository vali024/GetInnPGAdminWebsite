{
  "name": "get-inn-pg-admin",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd admin && npm install && cd ../backend && npm install",
    "build": "cd admin && npm run build",
    "start": "cd backend && node server.js",
    "dev": "cd admin && npm run dev"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
